// find the number that appear only once are may be odd time
// only catch it all other numbers must be in even number

#include<iostream>
#include<vector>
using namespace std;

int main() {
    vector <int> arr{1,1,2,5,2,3,3};
    int ans = 0;
    for (int i = 0; i < arr.size(); i++)
    {
        ans = ans ^ arr[i];
    }
    cout<<ans;
    
    return 0;
}
